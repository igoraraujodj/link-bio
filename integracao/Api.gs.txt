/* ═════════════════════════════════════════════════════════════════════════
   COLE ISTO NO SEU APPS SCRIPT

   Projeto: "Financas Casa"
   Arquivo: Código.gs   ← é o único arquivo .gs do projeto, tem tudo dentro

   ONDE EXATAMENTE:

   1. Abra o Código.gs e use Ctrl+F (ou Cmd+F) procurando por:

        createTemplateFromFile('Painel')

      Só existe uma ocorrência. Ela está dentro da função doGet.

   2. Cole o bloco abaixo na linha DE CIMA dessa, ou seja:
      logo ANTES de `const t = HtmlService.createTemplateFromFile('Painel');`

   3. Implantar → Gerenciar implantações → ícone de lápis → Versão: Nova
      versão → Implantar.

   NÃO altera nada do que já existe: o painel continua abrindo igual, o
   diagnóstico (?d=1) continua igual, o bot continua igual.
   ═════════════════════════════════════════════════════════════════════════ */

  // ?json=1 — mesma chave do painel, resposta em JSON para o app do casal.
  // ContentService e não HtmlService de propósito: só ele responde com o
  // cabeçalho de CORS que o navegador exige para ler de outro domínio.
  if (e.parameter.json) {
    return ContentService
      .createTextOutput(JSON.stringify(dadosPainel()))
      .setMimeType(ContentService.MimeType.JSON);
  }


/* ═════════════════════════════════════════════════════════════════════════
   DEPOIS DE COLAR

   No grupo do Telegram, mande /painel e copie o link que o bot responder.
   No app: Ajustes → Bot do Telegram (finanças) → Colar o link.

   Pronto. O ?json=1 o app acrescenta sozinho.
   ═════════════════════════════════════════════════════════════════════════ */


/* ═════════════════════════════════════════════════════════════════════════
   OPCIONAL — a coluna que falta para o acerto do mês

   Hoje a planilha guarda QUEM PAGOU (coluna `quem`), mas não DE QUEM É a
   despesa. Sem isso não dá para dizer "a Karen te deve R$ 200 esse mês".

   Tudo abaixo é no mesmo Código.gs.

   1. Na planilha, aba `lancamentos`, crie a coluna 14 com o título: rateio

   2. Procure por `const COL = {` e acrescente `rateio: 14` no fim do objeto.

   3. Procure por `function escrever_` e, dentro dela:
        • troque `getRange(linha, 1, 1, 13)` por `getRange(linha, 1, 1, 14)`
        • acrescente `d.rateio || 'meio'` como ÚLTIMO item do array

   4. Procure por `function gravarEConfirmar_` e acrescente uma segunda
      linha de botões ao teclado:

        [
          { text: '½ Meio a meio', callback_data: 'rat:' + linha + ':meio'  },
          { text: 'Só Igor',       callback_data: 'rat:' + linha + ':igor'  },
          { text: 'Só Karen',      callback_data: 'rat:' + linha + ':karen' }
        ]

   5. Procure por `function tratarBotao_` e trate a ação 'rat' do mesmo jeito
      que ele já trata a ação 'set' (gravar na coluna e responder).

   6. Procure por `function dadosPainel` e, no laço que monta os lançamentos:
        • troque `sh.getLastRow() - 1, 13` por `sh.getLastRow() - 1, 14`
        • acrescente mais um campo ao objeto:  r: String(r[13] || 'meio'),

   O app já lê esse campo `r` se ele existir, e ignora se não existir. Ou
   seja: pode deixar isso para depois, sem pressa, que nada quebra.
   ═════════════════════════════════════════════════════════════════════════ */
